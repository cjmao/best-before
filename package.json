{
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "migrations:generate": "drizzle-kit generate:sqlite",
    "migrations:list": "wrangler d1 migrations list best-before --local",
    "migrations:apply": "wrangler d1 migrations apply best-before --local"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.3",
    "drizzle-zod": "^0.5.1",
    "hono": "^3.12.3",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "drizzle-kit": "^0.20.12",
    "wrangler": "^3.22.0"
  }
}
