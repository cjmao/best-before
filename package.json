{
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --mode client",
    "dev": "vite build && vite build --mode client && wrangler dev --assets dist",
    "migrations:generate": "drizzle-kit generate:sqlite",
    "migrations:list:local": "wrangler d1 migrations list best-before --local",
    "migrations:list:preview": "wrangler d1 migrations list best-before-preview",
    "migrations:list:production": "wrangler d1 migrations list best-before",
    "migrations:apply:local": "wrangler d1 migrations apply best-before --local",
    "migrations:apply:preview": "wrangler d1 migrations apply best-before-preview",
    "migrations:apply:production": "wrangler d1 migrations apply best-before"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.1.11",
    "drizzle-orm": "^0.29.3",
    "drizzle-zod": "^0.5.1",
    "hono": "^3.12.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "@hono/vite-cloudflare-pages": "^0.1.2",
    "@types/react": "^18.2.48",
    "@types/react-dom": "^18.2.18",
    "drizzle-kit": "^0.20.12",
    "vite": "^5.0.12",
    "wrangler": "^3.22.0"
  }
}
